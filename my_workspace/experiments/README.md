# Experiments

Store each experiment as one subfolder, for example:

- `exp001_add_fundamentals/`
- `exp002_new_objective/`

Each experiment folder should include:

1. What changed.
2. Exact run command.
3. Validation metrics (rank IC, any custom metrics).
4. Output submission filename.
